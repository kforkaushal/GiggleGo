org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Fix for Windows cross-drive Kotlin incremental compilation crash.
# Kotlin's RelocatableFileToPathConverter cannot handle pub cache (C:) and
# project (D:) being on different drive letters. Disabling incremental
# compilation avoids the bug entirely. Builds will be ~30s slower but stable.
kotlin.incremental=false
